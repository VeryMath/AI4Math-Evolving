[project]
name = "openevolve-coding-agent-skill"
version = "0.1.0"
description = "Codex skill and scripts for OpenEvolve coding-agent workflows"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
