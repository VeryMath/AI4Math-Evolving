# OpenCode Loading Notes

This repository exposes a shared Skill layer at:

```text
openevolve-experiment-workflow/SKILL.md
```

OpenCode can use the repository checkout directly by reading `AGENTS.md`,
`SKILL.md`, and the concrete Skill file. If using a plugin-style wrapper, keep
the wrapper thin and point it back to the shared Skill layer.
