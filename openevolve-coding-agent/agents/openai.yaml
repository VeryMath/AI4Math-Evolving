interface:
  display_name: "AI4Math-Evolving Agent"
  short_description: "Interactively run and inspect OpenEvolve projects"
  default_prompt: "Use $openevolve-coding-agent to import or select this OpenEvolve project, validate it, configure a run, tail logs, and summarize the best metrics."
