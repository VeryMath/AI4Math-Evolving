interface:
  display_name: "AI4Math-Evolving Agent"
  short_description: "Goal-driven OpenEvolve sessions with agent feedback"
  default_prompt: "Use $openevolve-coding-agent to turn my evolving goal and feedback into an interactive OpenEvolve session: inspect the project, propose the next experiment, run only when ready, and summarize the best metrics."
